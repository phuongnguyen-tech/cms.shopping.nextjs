interface ICustomer {
    id: string
    firstName?: string
    lastName?: string
    email: string
    phone: string
    address?: string
    password: string
    createdAt: string
}

interface Customer {
    id: string
    name: string
}
